<h1>Subjects#delete</h1>
<p>Find me in app/views/subjects/delete.html.erb</p>

<%= link_to("<<back to list",{:action=>'index'},:class=>'back-link') %>
<div class="subjects delete">
	<h2>Delete Subjects </h2>
	 <%= form_for(:subject,:url=> {:action=>'destroy',:id => @subject.id}) do |f| %>
<%#= form_tag({:action=>'destroy',:id => @subject.id}) do %>

<h1><%= @subject.name %></h1>
	<div class="form-buttons">
		<%= submit_tag("Delete Subject") %>
	</div>
	<% end %>
</table>
